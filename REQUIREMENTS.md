# stt_translate_gpu 요구사항 명세

본 문서는 현재 저장소에 구현된 동작( `app/main.py`의 API, `WhisperService`, `TranslateUtil`, `lang_detect`)을 기반으로 작성한 **기능 요구사항** 및 **비기능 요구사항**입니다. 추후 제품 목표/사용 시나리오가 추가되면 갱신할 수 있습니다.

## 1. 용어 정의
- **STT**: Speech-to-Text. 음성을 텍스트로 변환하는 기능.
- **프롬프트(영문)**: STT 결과를 기반으로 영어(`en`)로 번역/정규화된 출력 문자열.
- **언어 감지**: STT 텍스트의 언어를 확률 기반으로 추정.
- **폴백(fallback)**: 품질 점수가 임계값 미만일 때 다른 번역 모델로 재번역하는 동작.

## 2. 기능 요구사항 (Functional Requirements)

### FR-01. STT-번역 파이프라인 제공
- 시스템은 클라이언트로부터 오디오 파일을 입력받아 다음 파이프라인을 수행해야 한다.
  - (1) STT로 텍스트 생성
  - (2) 언어 감지
  - (3) 감지된 언어(또는 감지 실패 상태)에 대해 영어로 번역하여 프롬프트 생성

### FR-02. REST API 엔드포인트 제공
- 시스템은 아래 엔드포인트를 제공해야 한다.
  - `POST /voice-to-prompt`
- 입력 형식은 `multipart/form-data`이며, 필드는 `file` 이어야 한다.

### FR-03. 입력 오디오 처리
- 서버는 요청으로 전달된 오디오 파일을 수신해야 하며,
  - 내부적으로 임시 저장소(`tmp/`)에 저장한 뒤 STT에 전달해야 한다.

### FR-04. 언어 감지 로직
- 시스템은 STT 결과 텍스트에 대해 언어 감지를 수행해야 한다.
- 언어 감지는 다음 조건을 만족할 때만 언어 코드를 반환해야 한다.
  - 텍스트 길이가 최소 길이 이상
  - 상위 언어가 대상 언어 목록에 포함
  - 확률이 임계값 이상

### FR-05. 번역 출력 조건
- 언어 감지 결과에 따라 영어 프롬프트 생성 로직을 달리한다.
  - 언어 감지 결과가 `en`이면 STT 텍스트를 그대로 프롬프트로 사용
  - 그 외 언어이면 해당 언어를 `src`로 두고 `tgt=en`으로 번역
  - 언어 감지 실패(`None`)인 경우에도 영어 프롬프트를 생성해야 한다.

### FR-06. “Smart Translate” 품질 검증 및 폴백
- 영어 번역은 `TranslateUtil.smart_translate()` 로 수행되어야 한다.
- `smart_translate()`은 back-translation 기반 품질 점수(임베딩 유사도)를 계산해야 하며,
  - 품질 점수가 임계값 미만인 경우 `nllb`로 재번역(fallback)해야 한다.

### FR-07. 응답 포맷
- `POST /voice-to-prompt` 호출에 대한 응답은 JSON이어야 하며, 다음 키를 포함해야 한다.
  - `detected_lang`: 감지된 언어 코드 또는 `None`
  - `stt_text`: STT 결과 텍스트
  - `prompt_en`: 영어 프롬프트 텍스트

### FR-08. 서버 CORS 정책 지원
- 브라우저 기반 클라이언트 접근을 위해 CORS 미들웨어를 제공해야 한다.
- 현재 구현 기준으로 `allow_origins=["*"]` 및 `allow_credentials=True` 정책이 적용되어야 한다.

### FR-09. 모델 로딩 및 캐싱
- STT 및 번역 모델은 서버 시작 시 로드되어야 하며,
- 번역 모델(Tokenizer/Model)은 재사용을 위해 메모리 캐싱되어야 한다.

### FR-10. 디바이스 선택
- STT 및 번역 실행 디바이스는 다음 기준으로 결정되어야 한다.
  - `cuda` 사용 가능하면 GPU 사용
  - 그렇지 않으면 CPU 사용

## 3. 비기능 요구사항 (Non-Functional Requirements)

### NFR-01. 성능(Performance)
- STT 및 번역은 가능한 경우 GPU(`cuda`)에서 동작해야 한다.
- 모델 로딩 및 번역 요청 처리 시간은 서비스 사용성을 고려하여 최적화되어야 한다.
  - 번역 모델 캐싱을 통해 반복 호출 시 지연을 줄여야 한다.

### NFR-02. 가용성(Availability)
- 서버는 요청 처리 중 예외가 발생할 수 있으므로, 다음을 만족해야 한다.
  - 최소한의 예외 로깅/추적을 통해 원인을 파악할 수 있어야 한다.
  - (권장) 번역/감지 실패 시에도 API는 가능한 범위에서 응답을 반환해야 한다.

### NFR-03. 신뢰성(Reliability)
- 동일 입력에 대해 재현 가능한 결과를 제공하도록 설정(모델/파라미터)을 고정해야 한다.
  - 예: 번역 생성 파라미터(`max_length`, `num_beams`)가 의도한 값으로 유지되어야 한다.

### NFR-04. 보안(Security)
- 임시 파일 저장 및 파일명 처리에 대해 보안 위협을 방지해야 한다.
  - (권장) 업로드된 `file.filename`을 경로 탐색에 안전하게 정규화/검증해야 한다.
- CORS 정책으로 인해 민감 데이터가 노출되지 않도록 정책을 검토해야 한다.

### NFR-05. 데이터 처리(Privacy)
- STT 처리에 필요한 최소한의 데이터만 사용해야 하며,
  - 업로드된 오디오 파일은 필요 기간 이후 삭제/정리되어야 한다. (현재 구현에는 삭제 로직이 없음)

### NFR-06. 호환성(Compatibility)
- FastAPI 기준으로 `multipart/form-data` 업로드가 동작해야 한다.
- Python/라이브러리 버전 조합에 따라 모델 로딩이 실패할 수 있으므로, 재현 가능한 실행 환경을 문서화해야 한다.

### NFR-07. 확장성(Scalability)
- 모델 로딩 비용과 메모리 사용량을 고려할 때,
  - 단일 인스턴스에서 동시 요청 증가에 대한 한계를 식별하고(예: 동시성 제어),
  - 필요 시 워커 수/큐잉 전략을 적용할 수 있어야 한다.

### NFR-08. 관측가능성(Observability)
- 번역 관련 품질 점수, 모델 선택 결과 등 주요 처리 단계는 로그로 남겨야 한다.
  - 현재 구현은 `logging`을 사용하므로, 운영 시 로그 레벨/형식을 조정할 수 있어야 한다.

### NFR-09. 유지보수성(Maintainability)
- 번역 모델/언어 매핑 및 폴백 로직은 설정 기반으로 관리되어야 하며(`app/config.py`),
  - 새 번역 모델 추가/교체가 가능해야 한다.

### NFR-10. 테스트 가능성(Testability)
- 최소한 다음 수준의 테스트/검증이 가능해야 한다.
  - API 단위 테스트(요청/응답 포맷 검증)
  - 언어 감지 임계값 변경 시 동작 변화 검증
  - 번역 폴백 조건(`SCORE_THRESHOLD`)에 따른 결과 검증

## 4. 수용 기준(초안)
- `POST /voice-to-prompt`에 유효한 오디오 파일을 전달하면 응답 JSON에
  - `stt_text`가 문자열로 존재
  - `prompt_en`이 문자열로 존재
  - `detected_lang`이 대상 언어 또는 `None`으로 존재해야 한다.
- 언어 감지 확률이 임계값을 넘는 경우에는 `detected_lang`이 설정된 언어 코드 중 하나로 반환되어야 한다.

## 5. 가정 및 제약(초안)
- 현재 구현은 대상 언어를 일부 언어(`ko/en/ja/zh/es/fr/de/it/pt`)로 한정한다.
- 번역 모델 선택 로직(`select_model`)은 현재 고정값(`m2m100`)을 반환한다.
- 임시 파일(`tmp/`) 정리 정책은 현재 구현에 포함되어 있지 않다.

